# 宿主机仅在本机发布 15300；需要通过反向代理或可信局域网访问时再改为 0.0.0.0。
HERMES_YAOYAO_BIND_ADDRESS=127.0.0.1
HERMES_YAOYAO_PUBLISHED_PORT=15300
# 可选：使用已经导入或发布的镜像；留空时使用本地构建名称。
# HERMES_YAOYAO_IMAGE=hermes-yaoyao:local
# YAOYAO_DESKTOP_IMAGE=hermes-yaoyao-desktop:0.4.0

# 部署步骤见 docs/docker-install.md；使用 docker compose --env-file docker.env 启动。
# 填写容器可访问的 Hermes Dashboard/Gateway 地址。
# Docker Desktop 可用这个地址访问宿主机；Linux 上的 9119 还必须监听容器可达的宿主机地址。
HERMES_YAOYAO_UPSTREAM=http://host.docker.internal:9119
HERMES_YAOYAO_CHAT_CACHE_MODE=prefer-local
# 首次登录后在 Web 系统设置中保存 Hermes 连接凭据。
# 使用 compose.desktops.yaml 可同时部署固定数量的共享桌面容器。
# 桌面数量以 Compose 为准，Web 仅连接已有桌面；详见 docs/docker-install.md。

# 使用域名访问时填写域名，多个值以英文逗号分隔；localhost 和私网 IP 无需填写。
HERMES_YAOYAO_ALLOWED_HOSTS=

# 可选 Android FCM：先把仓库外的服务账号 JSON 以只读方式挂载到 Web 容器，
# 再在 compose override 中把以下三项透传给 web 服务。不要把 JSON 复制进镜像或仓库。
# HERMES_YAOYAO_FCM_SERVICE_ACCOUNT_FILE=/run/secrets/firebase-service-account.json
# HERMES_YAOYAO_FCM_PROJECT_ID=your-firebase-project-id
# HERMES_YAOYAO_FCM_PACKAGE_NAME=cn.samien.yaoyao.hermes
