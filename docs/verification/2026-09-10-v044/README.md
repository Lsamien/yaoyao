# v0.4.4 验证

- 真实 Electron 独立窗口：本机测试服务授予控制租约，画面输入由服务器接收，系统关闭按钮先交还租约，主窗口保留。截图为交互测试画面，不表示真实虚拟机业务任务已完成。
- 数据迁移：覆盖旧默认目录、自定义目录、SQLite 活跃锁、账号密钥、Runner 状态、文件索引、更新事务延后、冲突保留和改名后的恢复。
- 已停止虚拟机旧挂载：验证所属节点、完整隔离配置与旧路径后保留原容器，重新挂载新目录下的工作文件。
- 本机完整数据备份后迁移成功：247 条工作区实体、240 条缓存消息，三个数据库完整性检查通过，账号和密钥摘要保持一致。
- Android 1.3（162）Release 构建与单元测试通过，APK 正式签名验证通过。
- 最终 Web 回归：897 通过、7 跳过；类型检查与生产构建通过。Electron 生命周期/升级等 13 项、原生钥匙串 1 项和独立接管窗口 1 项通过；Bot 侧栏桌面/手机浏览器流程 1 项通过。
