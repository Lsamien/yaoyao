# v0.3.23

- `source=web` 普通聊天在 15300 的独立 SQLite 数据库中持久化，会话列表、消息和附件优先本地读取。
- 9119 只在缓存缺失、事件变化、执行控制或校准时读取；完成消息会触发后台全量回填。
- 15300 重启或 9119 离线时，已完整同步的普通聊天和媒体仍可读取。
- Web 历史记录保持按需读取 9119，不写入持久化聊天缓存。
- 提供 `upstream-only`、`shadow` 和 `prefer-local` 三种服务端运行模式，默认 `prefer-local`。
