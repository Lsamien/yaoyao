# v0.3.19

补齐已有 Agent 首次安装头像规则。

- 已有 Agent 如果仍是系统生成的绿色圆形默认头像，会在安装此版本时一次性分配随机形状／造型、颜色和基础表情。
- 已上传的照片和已自定义的 v2 头像保持原样。
- 随机结果写入 Agent 记录及对应单聊快照；迁移完成后记录独立标记，重启不会再次变化。
- 新建 Agent 继续使用 v0.3.18 的创建时随机头像规则。

配套 iOS 继续为 1.3（177）。
