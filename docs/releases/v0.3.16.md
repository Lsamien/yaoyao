# v0.3.16

修复 Bot 模式设置中的身份链路，并明确区分账号与 Agent。

- Bot 模式加载与聊天模式相同的 Profile 名称和头像，避免回退显示 `default / default`。
- Bot 模式接通 Agent 身份保存事件、忙碌状态、错误信息和保存后刷新；造型、颜色、表情及名称可持久保存。
- 设置中心的账号页显示登录账号；Agent 身份和模型页显示当前正在设置的 Profile。
- 登录账号暂未提供自定义头像字段，使用统一的绿色圆形默认头像；各模式显示一致。

配套 iOS 继续为 1.3（176）。
