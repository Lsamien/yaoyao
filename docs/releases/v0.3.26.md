# v0.3.26

- 15300 按登录用户、Profile 和 Session ID 持久登记普通聊天所有权，不再仅依赖 Hermes 的 `source` 字段判断能否续聊。
- Web 会话列表合并服务端已登记的会话；即使 Hermes 的来源元数据滞后，iOS 新建的聊天也会立即出现在 Web，并可在两端继续。
- 保留原生历史只读边界，未被 15300 登记的 Hermes 会话仍不会被误判为可写聊天。

配套 iOS 1.3（183）。
